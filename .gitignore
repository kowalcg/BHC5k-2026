# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Google Drive
.tmp.drivedownload
.tmp.driveupload

# Microsoft Office temporary files
~$*
*.tmp

# Backup files
*~
*.bak
*.swp

# Don't track the bronte-dashboard folder we just created (we'll use the original file)
bronte-dashboard/

# Playwright MCP working files
.playwright-mcp/

# Email credentials — never commit App Passwords or SMTP secrets
press_email_config.json
email_config.json
email_config_greg.json

